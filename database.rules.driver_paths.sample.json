{
  "_comment": "Use nexride/database.rules.json as the canonical RTDB rules file for Firebase deploy. Driver repo database.rules.json mirrors the ride_requests subtree. Discovery path: ride_requests ordered by market_pool; riders set market_pool == canonical market slug until a driver is assigned, then clear market_pool."
}
